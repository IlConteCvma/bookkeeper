  
language: java

matrix:
  include:
    - os: linux
      arch: arm64
      jdk: openjdk11
      

cache:
  directories:
  - "$HOME/.m2"

addons:
  apt:
    packages:
    - libcppunit-dev

install:
  - if [ "${TRAVIS_CPU_ARCH}" == "arm64" ]; then
     sudo apt-get install maven;
    fi
addons: 
    sonarcloud: 
        organization: ilcontecvma
        token: 
            a7ce466934bb85810f1d0853d6c3ab5c4bdd9165

script: 
    
    - mvn clean test -Pjdk-without-javah,coverage
    - mvn sonar:sonar -Dsonar.projectKey=IlConteCvma_bookkeeper -Dmaven.javadoc.skip=true
    
  
